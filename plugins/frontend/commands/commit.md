---
description: "Create a digest-aware atomic frontend commit with quality gates."
argument-hint: "[commit message]"
---

Parse `$ARGUMENTS` for an optional commit message, then invoke the `frontend`
skill with the `commit` lane.

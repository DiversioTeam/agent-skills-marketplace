---
description: "Refresh existing repository docs and simplify agent instructions within the requested path."
---

Use the `repo-docs-generator` skill in **canonicalize** mode with `$ARGUMENTS`.
The optional path defaults to `.`; `--dry-run` proposes changes without edits.
The skill owns scope, model guidance, preservation, and verification rules.

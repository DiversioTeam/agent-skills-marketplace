---
description: "Create repository docs and short agent instructions from verified code and tooling."
---

Use the `repo-docs-generator` skill in **generate** mode with `$ARGUMENTS`.
The optional path defaults to `.`. The skill owns discovery, model guidance,
doc layering, verification, and permission boundaries.

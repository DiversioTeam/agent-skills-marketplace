---
description: "Check pending release scope using captured commits and verified PR evidence."
---

Use the `release-manager` skill in **check** mode with `$ARGUMENTS`.
Follow its shared captured-scope procedure and report version, pending PRs,
verified inclusion, and ambiguities. Do not create, publish, synchronize, or deploy.

---
description: "Create a release PR from captured source and base commits."
---

Use the `release-manager` skill in **create** mode with `$ARGUMENTS`, including
`--hotfix` or `--dry-run` when supplied. Follow its scope, version, merge-method,
and exact-head gate rules. PR creation does not authorize deployment.

---
description: "Implement Optimo Mixpanel events using the current identity and event contract."
argument-hint: "[event or task] [--dry-run]"
---

Use the `mixpanel-analytics` skill in **implement** mode for $ARGUMENTS.
Read the current operating guide and the skill's non-regression contract before
editing. `--dry-run` means proposal only: no file changes or outbound events.
Follow the skill's implementation reference, repository gates, and completion
report; do not reuse pre-#3203 identity or cron templates.

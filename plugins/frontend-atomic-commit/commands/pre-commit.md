Run the frontend pre-commit checklist using the repo-local digest.

## Steps

1. Run the `frontend-atomic-commit` skill's pre-commit checklist.
2. Report the result:
   - If all checks pass: "Pre-commit checks passed. Ready to commit."
   - If any check fails: Show the errors and suggest fixes.

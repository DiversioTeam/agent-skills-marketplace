{
  "name": "ci-status",
  "version": "0.1.0",
  "description": "Pi CI status with checkout-scoped job selection, exact GitHub job logs, CircleCI console output, interactive details, and auto-watch.",
  "keywords": ["pi-package", "ci", "github-actions", "circleci"],
  "files": ["extensions/", "tests/", "README.md"],
  "scripts": {
    "test": "node --experimental-vm-modules --test tests/ci-status.test.mjs"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*",
    "typebox": "*"
  }
}

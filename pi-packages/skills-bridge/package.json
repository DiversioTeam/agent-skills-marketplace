{
  "name": "skills-bridge",
  "version": "0.0.3",
  "description": "Selects marketplace skill roots through environment, config, or checkout discovery, then delegates plugin skill loading to native Pi discovery.",
  "keywords": ["pi-package", "skills", "bridge"],
  "files": ["extensions/", "tests/", "README.md"],
  "scripts": {
    "test": "node --experimental-vm-modules --test tests/skills-bridge.test.mjs"
  },
  "pi": {
    "extensions": ["./extensions/skills-bridge/index.ts"]
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  }
}
